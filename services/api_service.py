import requests

def consultar_dados_api_externa():
    # 1. Definir o endereço (URL) da API externa
    
    # 2. Fazer a requisição HTTP (geralmente do tipo GET)
    #    response = requests.get(url, headers=...)
    
    # 3. Validar se a API externa respondeu com sucesso (status code 200)
    #    Se der erro (ex: 404, 500), retornar mensagem "Erro ao consultar serviço externo"
    
    # 4. Converter a resposta da API para JSON (dicionário Python)
    
    # 5. Extrair e tratar apenas as informações que o seu sistema precisa
    
    # 6. Retornar os dados formatados
    pass    